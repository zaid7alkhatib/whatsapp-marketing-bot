import PlaceholderCard from "../components/PlaceholderCard";

function NotFoundPage() {
  return (
    <PlaceholderCard
      title="Page Not Found"
      description="The requested route does not exist in this frontend module."
    />
  );
}

export default NotFoundPage;

